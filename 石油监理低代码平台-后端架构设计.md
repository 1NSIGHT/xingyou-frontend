# 石油监理低代码平台 — 后端架构设计（工程级）

> 配套文档：[`石油监理低代码平台-架构设计方案.md`](./石油监理低代码平台-架构设计方案.md)（总体架构）
> 本文聚焦**后端怎么落地**：模块边界、认证实现、拦截器顺序、JSONB 实操坑、事务边界、测试策略。
> 前置：前端登录页已完成，接口契约已冻结（见第 5 章）。

---

## 一、结论先行

| 决策项 | 选择 | 一句话理由 |
|---|---|---|
| 架构形态 | **模块化单体** | 100 并发下微服务收益为零、成本全是你的 |
| 语言/框架 | **JDK 21 + Spring Boot 3.5.x** | 信创适配最成熟 + 低代码所需组件生态最全 |
| 模块边界 | **Maven 多模块 + ArchUnit 测试守护** | 边界不测就会烂，写了 ArchUnit 才叫"模块化" |
| ORM | **MyBatis-Plus 3.5.x**（不用 JPA） | 低代码平台的核心是动态 SQL，Hibernate 会一路跟你对着干 |
| 认证 | **JWT 短期令牌 + Redis 刷新令牌/黑名单** | 对齐已完成的前端；补充说明见 5.5 |
| 租户隔离 | **共享库共享表 + tenant_id 拦截器** | 100 并发用共享表足够，且跨项目统计是刚需 |
| 数据权限 | **框架强制注入 SQL 谓词** | 靠业务代码记得加权限条件 = 迟早漏 |
| 动态数据 | **固定表 + JSONB + 生成列 + 展平宽表** | 详见总体方案第 4.2 节 |
| 流程引擎 | **Flowable 7.x 内嵌，与业务共享事务** | 一次提交要同时写单据和起流程，必须同事务 |
| 缓存 | **Caffeine（元数据）+ Redis（字典/权限/令牌）** | 元数据是最高频读取，必须本地缓存 |
| 部署 | **单体单进程 + Docker，2 节点无状态** | 详见总体方案第 9.1 节 |

**一句话**：后端只有一个应用进程，但内部必须是**边界清晰、可测试、可演进**的模块集合。你买的不是"分布式"，是"未来能拆"的选项。

---

## 二、Maven 模块划分（务实起步版）

### 2.1 不要一上来就切 12 个模块

架构方案里列的 12 个模块是**目标态**，不是起步态。绿地项目最常见的失败是过度模块化：构建变慢、IDE 卡、改一个功能要动 4 个 pom，团队怨声载道然后退化成互相乱依赖。

**起步只建 5 个模块**，按需增长：

```
xy-parent (pom)                       统一版本管理（BOM）
│
├── xy-common                         零业务依赖的基础设施
│     统一响应体 / 业务异常 / 错误码 / 基础实体 / 工具类 / 审计注解
│     ★ 禁止依赖任何其他模块，禁止依赖 Spring Web 之外的东西
│
├── xy-framework                      横切关注点（有 Spring 依赖，无业务语义）
│     Spring Security 配置 / JWT / 多租户拦截器 / 数据权限拦截器
│     MyBatis-Plus 配置 / 全局异常处理 / Redis 封装 / 缓存管理 / Jackson 配置
│     ★ 只依赖 xy-common
│
├── xy-modules                        业务模块聚合（pom）
│     ├── xy-system                   组织、用户、角色、权限、项目、字典  ← 第一个做
│     ├── xy-meta                     元数据引擎（表单定义、字段索引、编码规则、版本）
│     ├── xy-form                     表单数据引擎（动态单据 CRUD、校验、动态查询）
│     ├── xy-flow                     流程引擎封装（Flowable）
│     ├── xy-file                     文件、照片、签名
│     └── xy-supervision              行业业务（焊口台账、质量控制点、见证取样…）
│
└── xy-boot                           启动模块：配置、main()、Flyway 迁移脚本
```

**演进节奏**：

| 里程碑 | 需要建的模块 |
|---|---|
| M1 跑通登录 | `xy-common` `xy-framework` `xy-system` `xy-boot` |
| M2 跑通一个真实单据 | 增加 `xy-meta` `xy-form` |
| M3 审批流 | 增加 `xy-flow` |
| M4 附件/照片 | 增加 `xy-file` |
| M5 行业能力 | 增加 `xy-supervision` |

`xy-report` 等暂时**不要建空模块**——空模块只会让人困惑。等真要做报表时再拆。

### 2.2 依赖方向（单向，不可逆）

```
xy-boot  ──→  xy-modules/*  ──→  xy-framework  ──→  xy-common
                    │
                    └──→  xy-common（可直接用基础工具）
```

业务模块之间：**允许 `xy-form` 依赖 `xy-meta`**（表单数据引擎天然要读元数据），但**禁止反向**。
`xy-supervision` 可以依赖 `xy-form`（行业插件往表单引擎注册校验规则），但 `xy-form` **绝不能**依赖 `xy-supervision`——扩展到新行业时不能改内核。

跨模块通信的正确方式：
1. **接口下沉**：需要被内核调用的业务能力，接口定义在 `xy-form`，实现放在 `xy-supervision`（SPI 模式）
2. **领域事件**：`xy-supervision` 监听 `xy-form` 发布的 `DocumentSubmittedEvent`，Spring `@TransactionalEventListener`

### 2.3 用 ArchUnit 把边界变成可执行断言

不测的架构约束等于没有。`xy-boot` 里加一个测试类，CI 里跑：

```java
@AnalyzeClasses(packages = "com.xingyou")
class ArchitectureTest {

    @ArchTest
    static final ArchRule 模块间依赖必须单向 = slices()
        .matching("com.xingyou.(*)..")
        .should().beFreeOfCycles();

    @ArchTest
    static final ArchRule 内核不得依赖行业业务 = noClasses()
        .that().resideInAnyPackage("com.xingyou.form..", "com.xingyou.meta..", "com.xingyou.flow..")
        .should().dependOnClassesThat().resideInAnyPackage("com.xingyou.supervision..");

    @ArchTest
    static final ArchRule common不得依赖框架和业务 = noClasses()
        .that().resideInAPackage("com.xingyou.common..")
        .should().dependOnClassesThat()
        .resideInAnyPackage("com.xingyou.framework..", "com.xingyou.system..", "com.xingyou.form..");

    @ArchTest
    static final ArchRule Controller不得直接被Mapper调用 = noClasses()
        .that().resideInAPackage("..controller..")
        .should().dependOnClassesThat().resideInAPackage("..mapper..");
}
```

**这几行代码是"模块化单体"和"一坨大泥球"的唯一区别。** 没有它，半年后 `xy-meta` 里会出现 `xy-supervision` 的 import，然后所有人都开始抄近路。

### 2.4 包结构约定（每个业务模块内部一致）

```
com.xingyou.system
├── controller/          只做参数校验 + 调 service + 包装响应，禁止业务逻辑
├── service/             业务编排 + 事务边界（@Transactional 只在这一层）
│   └── impl/
├── domain/              领域实体 + 值对象 + 领域服务（跨实体的业务规则）
├── mapper/              MyBatis-Plus Mapper 接口
├── entity/              数据库实体（与表一一对应，不进 Controller）
├── dto/
│   ├── request/        入参（带校验注解）
│   └── response/       出参
├── convert/             MapStruct 转换器（entity ↔ dto）
├── event/               领域事件定义
└── enums/
```

**硬规矩**：
- `entity` 绝不直接返回给前端（会泄露字段、且无法演进）
- `@Transactional` **只出现在 service 层**，Controller 和 Mapper 上不许有
- Mapper 只做数据访问，不许写 if-else 业务分支

---

## 三、ORM 选型：为什么是 MyBatis-Plus 而不是 JPA

这是低代码平台最关键的选型之一，选错了会持续痛苦两年。

| 维度 | MyBatis-Plus | JPA / Hibernate |
|---|---|---|
| 动态条件查询 | ★ 原生强项，`LambdaQueryWrapper` + 自定义 `InnerInterceptor` 拼接 | Criteria API 冗长，动态条件写得想死 |
| 低代码"用户自定义查询" | ★ 能精确控制生成的 SQL，可控可审计 | 生成 SQL 不可预测，难以做白名单校验 |
| 多租户自动注入 | ★ `TenantLineInnerInterceptor` 成熟 | 需 Hibernate Filter，配置繁琐 |
| 数据权限 SQL 谓词注入 | ★ 拦截器直接改 SQL | 困难 |
| JSONB 字段映射 | 自定义 `TypeHandler` 即可 | `@JdbcTypeCode` 支持尚可 |
| 分页 | `PaginationInnerInterceptor`（支持多数据库方言，信创友好） | 需自行处理 |
| 级联/对象图加载 | 手工（更透明） | ★ 自动（但也是 N+1 的重灾区） |
| 团队上手成本 | ★ 国内普及度极高 | 较高 |

**决策：MyBatis-Plus。** 低代码平台的本质是"元数据驱动生成动态 SQL"，这与 MyBatis 的设计哲学完全契合；JPA 的强项（对象图映射、级联、脏检查）在元数据驱动场景下几乎用不上，而它的弱项（动态 SQL）恰好是你的核心诉求。

**注意**：不要为了省事引入 `MyBatis-Plus-Join` 之类的第三方连表插件当主力，复杂查询老老实实写 XML——生成出来的 SQL 你要能为性能负责。

---

## 四、MySQL/PostgreSQL 之外：数据库方言抽象（信创预案）

信创要求下达梦 DM8 / 人大金仓 KingbaseES 会带来真实的兼容问题。**从第一天就做的两件事**：

1. **所有 SQL 走 MyBatis XML，不写数据库特有语法**。PostgreSQL 的 `JSONB` 操作符（`->>` `@>`）在达梦里未必存在——把 JSON 取值封装成自定义函数或统一在 TypeHandler 层处理，不要散落在 XML 里。
2. **Flyway 迁移脚本按方言分目录**：

```
xy-boot/src/main/resources/db/migration/
├── common/          通用 DDL（表、索引、约束）
├── postgresql/      仅 PG 的生成列、GIN 索引、部分索引
└── dameng/          达梦替代实现（可能退化为普通列 + 应用层维护）
```

**Spike 阶段必须实测的三件事**（写进总方案的验证清单）：
- 达梦/金仓是否支持**函数索引**与**部分索引**（不支持则"生成列"方案要整体退化为"应用层双写索引表"）
- JSON 存取的性能（达梦的 JSON 支持弱于 PG，可能要在应用层缓存）
- `PaginationInnerInterceptor` 的达梦方言是否可用

> 这一章不是吓唬你，是让你在 Spike 阶段（3 周）把风险暴露掉，而不是在交付前两周发现要重做数据层。

---

## 五、认证与授权实现（对齐已完成的前端）

前端 `src/api/auth.ts` 已经定义了契约，后端照着实现即可。

### 5.1 需要实现的 5 个接口

| 方法 | 路径 | 入参 | 出参（`data` 内） |
|---|---|---|---|
| GET | `/api/auth/captcha` | — | `{ captchaKey, captchaImage }`（图片为 base64 PNG） |
| POST | `/api/auth/sms-code` | `{ mobile }` | `void` |
| POST | `/api/auth/login` | `LoginParams` | `{ accessToken, refreshToken, expiresIn }` |
| GET | `/api/auth/me` | — | `UserInfo` |
| POST | `/api/auth/logout` | — | `void` |

**统一响应体**（前端拦截器已按此解包）：

```json
{ "code": 0, "message": "ok", "data": { } }
```

`code = 0` 成功；`code = 401` 触发前端清令牌并跳登录；其余为业务错误码。

### 5.2 图形验证码：服务端实现（替换前端 Canvas）

前端目前是本地 Canvas 生成（`VITE_USE_MOCK=true` 下的临时方案），**上线必须换服务端**，否则验证码形同虚设。

```
GET /api/auth/captcha
  ├─ 生成 4 位随机码（排除 0 O 1 I l）
  ├─ captchaKey = UUID
  ├─ Redis: SET auth:captcha:{captchaKey} = code EX 300     ← 5 分钟有效
  └─ 返回 { captchaKey, captchaImage: base64Png }

POST /api/auth/login
  ├─ GETDEL auth:captcha:{captchaKey}      ← ★ 一次性，取完即删（防重放/爆破）
  ├─ 校验（忽略大小写），失败 → 刷新让用户重填
  └─ 继续账号校验
```

**必须配套的三道限流**（否则验证码毫无意义）：

| 维度 | 策略 |
|---|---|
| 按 IP | 1 分钟最多 10 次登录请求（Nginx + 应用层双层） |
| 按账号 | 连续失败 5 次 → 锁定 15 分钟（Redis 计数） |
| 按验证码 | 同一 `captchaKey` 只允许校验 1 次（GETDEL 天然保证） |

前端接入时把 `GraphicCaptcha.vue` 的本地绘制换成 `<img :src="captchaImage">`，`captchaKey` 由接口下发（登录页里目前硬编码的 `'local-canvas'` 要一并替换，README 已注明）。

### 5.3 令牌设计

```
accessToken   JWT，有效期 2h
              claims: { sub: userId, tid: tenantId, jti, roles }
              ★ 不放敏感信息，JWT 只是签名不是加密，payload 谁都能解

refreshToken  不透明随机串（非 JWT），有效期 7d
              Redis: auth:refresh:{token} → userId, EX 7d
              ★ 用随机串而非 JWT，因为必须能即时吊销
```

**登出**：删除 refreshToken + 把 accessToken 的 `jti` 写入黑名单 `auth:blacklist:{jti}`，TTL = accessToken 剩余有效期（过期自动清理，不会无限增长）。

**令牌校验过滤器**（`xy-framework`）：

```java
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest req, HttpServletResponse resp, FilterChain chain) {
        String token = resolveBearer(req);
        if (StringUtils.hasText(token)
                && !redis.hasKey("auth:blacklist:" + jtiOf(token))   // ★ 黑名单校验
                && jwtValidator.isValid(token)) {
            SecurityContextHolder.getContext().setAuthentication(buildAuth(token));
        }
        chain.doFilter(req, resp);   // 无令牌不拦，交给授权规则决定
    }
}
```

Spring Security 6 配置要点（新式 Lambda DSL，`WebSecurityConfigurerAdapter` 已废弃）：

```java
@Bean
SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    return http
        .csrf(AbstractHttpConfigurer::disable)          // 前后端分离 + Bearer 令牌
        .sessionManagement(s -> s.sessionCreationPolicy(STATELESS))
        .authorizeHttpRequests(auth -> auth
            .requestMatchers("/api/auth/login", "/api/auth/captcha",
                             "/api/auth/sms-code", "/api/auth/refresh").permitAll()
            .requestMatchers("/actuator/health").permitAll()
            .anyRequest().authenticated())
        .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class)
        .exceptionHandling(e -> e
            .authenticationEntryPoint(restAuthEntryPoint)   // ★ 返回 JSON 401，不是重定向到登录页
            .accessDeniedHandler(restAccessDeniedHandler))  // ★ 返回 JSON 403
        .build();
}
```

**`authenticationEntryPoint` 必须自定义**——默认实现会返回 302 重定向，前端 axios 拿到的是 HTML，体验极差。这是最常见的踩坑点。

### 5.4 密码存储与等保要求

- **BCrypt**（强度 10）或 Argon2id。绝不用 MD5/SHA1/明文
- 等保三级通常要求：密码长度 ≥8、含大小写+数字+符号中的 3 类、90 天强制改密、不可与最近 3 次重复
- 登录日志留存 ≥6 个月（谁、何时、IP、UA、成功/失败）

### 5.5 一个诚实的补充：JWT 未必是最优解

既然前端已经按 JWT 实现了，继续用没问题。但你应该知道另一个选项：

| 方案 | 优点 | 缺点 |
|---|---|---|
| **JWT + Redis 黑名单**（当前） | 无状态、易水平扩展、移动端友好 | 吊销需黑名单兜底；令牌泄露在过期前有效；前端存储有 XSS 面 |
| **Redis 服务端 Session** | 即时吊销、无令牌泄露面（配合 httpOnly Cookie） | 每请求查一次 Redis（100 并发下毫无压力） |

**在 100 并发、内网部署的场景下，Redis Session 其实更简单也更安全**（不需要刷新令牌那套复杂度，也不需要黑名单）。

我建议保持 JWT 的理由：移动端离线场景友好、将来对接业主 SSO 更顺、前端已实现。但这是一个**可以推翻的决定**——如果你更看重"踢人下线要立即生效"，现在换成 Redis Session 的成本很低（改 `xy-framework` 一个过滤器和前端拦截器）。

---

## 六、多租户与数据权限：拦截器顺序是个坑

### 6.1 正确的注册顺序

MyBatis-Plus 多个 `InnerInterceptor` 的**注册顺序直接影响生成的 SQL**：

```java
@Bean
public MybatisPlusInterceptor mybatisPlusInterceptor() {
    MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

    // ① 多租户：必须最先，它改的是表名/追加 tenant_id 条件
    interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(tenantHandler()));

    // ② 数据权限：追加 project_id / section_id 等行级谓词
    interceptor.addInnerInterceptor(new DataScopeInnerInterceptor());

    // ③ 分页：★ 必须排在 ①② 之后
    //    因为分页会基于当前 SQL 生成 count 语句，
    //    如果它先执行，count 语句里就没有租户和数据权限条件 → 数量对不上、且越权统计
    interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.POSTGRE_SQL));

    // ④ 防全表更新删除：兜底保护，放最后
    interceptor.addInnerInterceptor(new BlockAttackInnerInterceptor());

    return interceptor;
}
```

**第 ③ 点的顺序错了会出很隐蔽的 bug**：列表数据正确但总数偏大，分页越翻越乱。而且这种 bug 在开发环境数据少时看不出来。

### 6.2 租户拦截器的白名单

系统表（字典、菜单、表单定义等全局共享的表）不能被追加 `tenant_id` 条件：

```java
public class XyTenantLineHandler implements TenantLineHandler {
    /** 全局共享、不参与租户隔离的表 */
    private static final Set<String> IGNORE_TABLES = Set.of(
        "sys_dict", "sys_dict_item", "sys_menu", "sys_permission",
        "meta_form", "meta_field_index",      // 表单定义全局共享，数据本身才隔离
        "flow_definition", "flyway_schema_history"
    );

    @Override
    public Expression getTenantId() {
        Long tenantId = SecurityUtils.getTenantId();
        // ★ 拿不到租户时直接抛异常，绝不能返回 null 或 0
        //   返回 null 会让 SQL 变成 tenant_id IS NULL → 全表查不到或越权
        if (tenantId == null) throw new BizException(ErrorCode.NO_TENANT_CONTEXT);
        return new LongValue(tenantId);
    }

    @Override
    public boolean ignoreTable(String tableName) {
        return IGNORE_TABLES.contains(tableName.toLowerCase());
    }
}
```

**「拿不到租户就抛异常」这一条是安全红线。** 一个常见的严重漏洞就是拦截器在无租户上下文时静默跳过条件，导致跨租户数据泄露。

### 6.3 数据权限：编译成 SQL 谓词

```java
@DataScope(resource = "biz_document", projectField = "project_id")
public IPage<DocumentVO> page(Query query) { ... }
```

拦截器读取注解 + 当前用户的权限规则，编译成谓词追加到 WHERE：

```sql
AND project_id IN (SELECT project_id FROM sys_project_member WHERE user_id = ? AND deleted = false)
```

**核心原则：权限谓词由框架注入，业务代码无法绕过。** 任何"在 service 里记得加 `and projectId = xxx`"的设计都是错的——总有一天有人忘。

**要做权限测试**：写一个基类，用两个不同租户/项目的账号去调每一个查询接口，断言互相看不到数据。这是最容易出安全事故的地方，值得单独立一套测试。

---

## 七、JSONB 落地：四个必踩的坑

### 7.1 坑一：PostgreSQL 写入 jsonb 会报类型错误

PG JDBC 驱动把 Java `String` 当 `varchar` 发送，插入 `jsonb` 列会报
`column "data_json" is of type jsonb but expression is of type character varying`。

**两个解法，选一个**：

```properties
# 解法 A：JDBC URL 加 stringtype=unspecified（全局生效，最省事）
spring.datasource.url=jdbc:postgresql://.../xy?stringtype=unspecified
```

```java
// 解法 B：自定义 TypeHandler 用 PGobject 显式指定类型（更可控，推荐）
@MappedTypes(Object.class)
@MappedJdbcTypes(JdbcType.OTHER)
public class JsonbTypeHandler extends BaseTypeHandler<Object> {
    private static final ObjectMapper MAPPER = new ObjectMapper();

    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, Object param, JdbcType t) throws SQLException {
        PGobject pg = new PGobject();
        pg.setType("jsonb");
        pg.setValue(toJson(param));
        ps.setObject(i, pg);        // ★ 必须用 setObject，不是 setString
    }

    @Override
    public Object getNullableResult(ResultSet rs, String col) throws SQLException {
        String raw = rs.getString(col);
        return raw == null ? null : fromJson(raw);
    }
    // ... 其余重载
}
```

配合实体上的：

```java
@TableName(value = "biz_document", autoResultMap = true)   // ★ autoResultMap 必须为 true
public class DocumentEntity {
    @TableField(value = "data_json", typeHandler = JsonbTypeHandler.class)
    private Map<String, Object> dataJson;
}
```

**注意 `autoResultMap = true`** —— 忘了它，查询时自定义 TypeHandler 不生效，读出来是字符串或 null。

### 7.2 坑二：生成列的不可变性（已在总体方案中修正）

`GENERATED ALWAYS AS (...)` 只接受 **IMMUTABLE** 表达式：

| 表达式 | 可用性 |
|---|---|
| `data_json->>'field'` | ✅ `jsonb_object_field_text` 是 immutable |
| `(data_json->>'amount')::numeric` | ✅ `numeric_in` 是 immutable |
| `(data_json->>'flag')::boolean` | ✅ `boolin` 是 immutable |
| `(data_json->>'date')::date` | ❌ `date_in` 是 **STABLE**（依赖 DateStyle），建表直接报错 |
| `(data_json->>'ts')::timestamp` | ❌ 同上 |

**日期字段的正确处理**：ISO-8601 文本（`YYYY-MM-DD`）的**字典序等于时间序**，所以直接把 text 生成列拿来排序和范围扫描即可，性能等同原生日期列。确需 timestamp 类型时，才用总体方案里那个 `to_ts_immutable()` 包装函数（前提是 DateStyle 固定为 `ISO, YMD`）。

### 7.3 坑三：JSONB 聚合会拖垮报表

```sql
-- ✘ 不要这样做
SELECT data_json->>'professional' AS p, count(*)
FROM biz_document WHERE form_def_id = 12
GROUP BY data_json->>'professional';
```

几十万行时这个查询会明显变慢（无法有效利用索引做聚合）。

**必须走展平宽表**：`xy-job` 定时 ETL 把 JSONB 展平成 `rpt_xxx` 普通列宽表，报表和驾驶舱查宽表。

```
biz_document (JSONB)
      │  xy-job 增量 ETL（每 5 分钟）/ 全量重建（每日凌晨）
      ▼
rpt_document_{formKey}   ← 列 = 该表单声明的"索引字段" + 通用列
      │
      ├──→ 积木报表 / ECharts / Excel 导出
      └──→ 只查这里，永不聚合 JSONB
```

**增量策略**：`biz_document` 加 `updated_at` 索引，ETL 按 `updated_at > last_watermark` 拉取；软删除也要同步到宽表。

### 7.4 坑四：JSONB 更新是整列覆盖

`UPDATE biz_document SET data_json = ?` 会**覆盖整个 JSON**。并发编辑同一单据时，后提交的会丢掉先提交的字段修改（即使改的是不同字段）。

**处理方式**：
- 单据级乐观锁：`UPDATE ... WHERE id = ? AND version = ?`，`version` 自增，冲突返回 409
- 前端提示用户"该单据已被他人修改，请刷新后重试"
- **不要在数据库层用 `jsonb_set` 做字段级合并** —— 那会让乐观锁失效，且需要多次往返

这和前端登录页一样，属于"简单方案足够"的典型场景：监理单据的并发编辑冲突极少，乐观锁完全够用，不要上 CRDT 或字段级 diff 合并。

---

## 八、元数据引擎：编译流水线

这是整个平台的心脏。**"发布"这个动作的本质是"编译"**。

```
【设计态】POST /api/design/form/{id}/publish  （携带 schema JSON）
    │
    ├─ ① 校验 schema 合法性（组件类型白名单、fieldKey 格式、无重复 key）
    ├─ ② 提取字段清单 → meta_field_index
    │     对每个字段记录：fieldKey / type / required / unique / searchable / indexable
    ├─ ③ 编译校验规则 → 后端可执行的规则 DSL（存 meta_form_version）
    ├─ ④ 为 searchable 字段生成 DDL（生成列 + 索引）
    │     ★ 这是唯一会执行 DDL 的地方，且在事务外单独执行（DDL 会隐式提交）
    ├─ ⑤ 生成列表/详情视图默认 schema
    ├─ ⑥ 生成权限点（formKey:read / formKey:create / formKey:field:{k}:read）
    ├─ ⑦ 生成编码规则
    └─ ⑧ 写入 meta_form_version（version+1，不可变快照）
    │
    └─ ⑨ 失效元数据缓存（本地 Caffeine + Redis pub/sub 通知其他节点）
```

**四个必须守住的点**：

1. **DDL 与元数据写入不能放在同一个事务里**。PostgreSQL 的 DDL 支持事务，但你的迁移要兼容达梦，且 DDL 失败时的回滚语义各库不一。做法：先写元数据（记录 `ddl_status = PENDING`），再独立执行 DDL，成功后更新为 `READY`。失败则整个发布回滚到草稿态。

2. **版本快照不可变**。`meta_form_version` 一旦写入就不能改。单据实例存 `form_def_version`，永远按绑定的旧版本渲染。

3. **索引字段数量上限**。给用户一个明确配额（如单表单最多 20 个可索引字段），UI 上提示。不限制的话，用户会把所有字段都勾上，然后插入性能崩掉。

4. **回滚 = 新建版本，不是删除**。回滚到 v1 的正确做法是生成 v3（内容等于 v1），而不是删掉 v2——否则 v2 的历史单据会失去定义。

**缓存失效要跨节点**：2 个应用节点各有本地 Caffeine 缓存，发布后要用 Redis pub/sub 广播失效消息，否则另一个节点的用户还在用旧 schema。

---

## 九、服务端校验引擎

前端 amis 的校验**只做体验**，后端必须按 schema 全量重跑。低代码平台的前端是"用户可配置的"，等于完全不可信。

**规则用 JSON DSL 存储，不用 Groovy/JS 动态脚本**（安全 + 性能 + 可控）：

```json
{
  "fieldKey": "weldDate",
  "type": "date",
  "required": true,
  "requiredWhen": "status == 'SUBMITTED'",
  "rules": [
    { "rule": "notBefore", "ref": "startDate", "message": "焊接日期不得早于开工日期" },
    { "rule": "notAfter",  "value": "today",    "message": "焊接日期不得晚于今天" }
  ]
}
```

校验器接口（`xy-form`）：

```java
public interface FieldValidator {
    String supportedType();                       // date / numeric / text / dict ...
    List<ValidationError> validate(FieldDef def, Object value, Map<String,Object> allValues);
}
```

行业插件通过实现这个接口注册自定义规则（SPI）：

```java
// xy-supervision 提供：A 级质量控制点必须有旁站记录
@Component
public class QcPointValidator implements CrossFieldValidator {
    public String name() { return "qcPoint.requireWitness"; }
    // ...
}
```

`xy-form` 启动时用 Spring 收集所有 `FieldValidator` 实现，`xy-supervision` 引入即生效——**内核不依赖行业模块，符合 2.2 节的依赖方向**。

**校验顺序**（快速失败，避免无谓计算）：
1. 结构校验（字段是否属于该 formKey、类型是否匹配）
2. 单字段校验（必填、长度、正则、范围）
3. 字典有效性
4. 跨字段校验
5. 唯一性（查库，最贵，放最后）

---

## 十、动态查询构造器（安全关键）

用户在列表设计器里配的查询条件 → 后端生成 SQL。**这是最容易被 SQL 注入的地方。**

```java
public class DynamicQueryBuilder {

    private static final Pattern FIELD_KEY = Pattern.compile("^[a-zA-Z][a-zA-Z0-9_]{0,63}$");
    private static final Set<String> ALLOWED_OPS =
        Set.of("EQ", "NE", "GT", "GE", "LT", "LE", "IN", "NOT_IN", "LIKE", "BETWEEN", "IS_NULL");

    public QueryWrapper<DocumentEntity> build(Long formDefId, QuerySpec spec) {
        QueryWrapper<DocumentEntity> w = new QueryWrapper<>();

        for (Condition c : spec.getConditions()) {
            // ① 字段必须在 meta_field_index 登记，且属于当前 formKey
            FieldIndex f = fieldIndexService.require(formDefId, c.getField());   // 未登记 → 抛异常
            // ② fieldKey 必须匹配白名单正则（额外防线）
            if (!FIELD_KEY.matcher(f.getFieldKey()).matches()) throw new BizException(ILLEGAL_FIELD);
            // ③ 操作符必须在枚举内
            if (!ALLOWED_OPS.contains(c.getOp())) throw new BizException(ILLEGAL_OP);
            // ④ 值按字段类型强转，全程参数化绑定（#{} 而非 ${}）
            Object v = typeCoerce(f.getType(), c.getValue());
            appendCondition(w, f, c.getOp(), v);
        }
        return w;
    }
}
```

**四条防线，一个都不能少**：

1. 字段名必须来自 `meta_field_index`（未登记字段直接拒绝）
2. `fieldKey` 正则白名单（纵深防御）
3. 操作符枚举白名单
4. **全程 `#{}` 参数化，禁止 `${}` 拼接** —— 这条在 code review 里要重点盯，MyBatis 的 `${}` 是注入的直接入口

另外：**排序字段**同样要白名单（很多人只防了查询条件，忘了 `ORDER BY`）。**LIKE 查询**要转义 `%` 和 `_`。

---

## 十一、事务边界与领域事件

### 11.1 一次单据提交涉及什么

```
POST /api/form/{formKey}/submit
    │  @Transactional  ← 事务边界在 Service 层
    ├─ 幂等检查（clientRequestId 唯一索引）
    ├─ 元数据校验
    ├─ 写 biz_document            ← 同一事务
    ├─ 写 biz_document_item       ← 同一事务
    ├─ 启动 Flowable 流程实例      ← ★ 必须同一事务
    └─ 写审计日志
    │
    └─ 事务提交后（@TransactionalEventListener(AFTER_COMMIT)）
       ├─ 写 biz_todo 待办视图表
       ├─ 更新待办数量缓存
       ├─ 发通知（站内信/短信/企微）
       └─ 发布 DocumentSubmittedEvent → 行业模块同步台账
```

**要点**：

1. **Flowable 必须与业务同事务**。Flowable 的 `SpringProcessEngineConfiguration` 接入了 Spring 的 `PlatformTransactionManager`，所以只要在同一个 `@Transactional` 里启动流程，就会一起提交/回滚。**如果单据写成功了但流程没起来，这张单据就永远卡在"草稿"状态且无法自愈**——这是必须避免的。

2. **事务后的副作用用 `AFTER_COMMIT`**。发通知、写待办视图表这些不能放在事务内，否则事务回滚了通知却发出去了；而且外部 IO 会拉长事务持有时间。

3. **不要在事务里调外部 HTTP**。第三方接口超时会占着数据库连接不放。

### 11.2 幂等

前端离线填报会重放提交请求，所以幂等是**刚需**不是优化：

```sql
-- 单据表加唯一索引
CREATE UNIQUE INDEX uk_doc_client_req ON biz_document (tenant_id, client_request_id)
    WHERE client_request_id IS NOT NULL;
```

服务端先查 `clientRequestId` 是否已存在，存在则直接返回上次的结果（而不是报错）。**离线重放必须幂等**，否则现场网络抖动会产生重复单据。

### 11.3 事务注意事项

- `@Transactional` **不要加在 Controller 上**（会包住参数校验和序列化）
- **同类内部方法调用不走代理**，`this.method()` 上的 `@Transactional` 不生效——这是最常见的失效场景
- 默认只对 `RuntimeException` 回滚，受检异常要显式 `rollbackFor = Exception.class`
- 事务里**不要做长耗时操作**（批量导入分批提交，不要一个大事务跑 10 分钟）

---

## 十二、缓存策略

| 数据 | 位置 | 失效方式 | 说明 |
|---|---|---|---|
| 表单定义 schema | Caffeine（本地）+ Redis | 发布时 Redis pub/sub 广播 | 最高频读取，必须本地缓存 |
| 数据字典 | Redis | 字典变更时删 key | TTL 1h 兜底 |
| 用户权限 | Redis | 角色变更 / 登录时 | TTL 30min |
| 在线用户 / 令牌 | Redis | — | — |
| 统计报表 | **不缓存** | — | 查展平宽表已经够快，缓存反而带来一致性问题 |

**多级缓存的坑**：本地 Caffeine 缓存**没有跨节点失效能力**。发布表单定义后如果只清本地缓存，另一个节点的用户还会用旧 schema 长达几分钟。

正确做法：

```java
// 发布成功后广播
redisTemplate.convertAndSend("meta:cache:evict", "form:" + formKey);
// 各节点订阅后清自己的 Caffeine
```

---

## 十三、Flowable 集成要点

| 要点 | 说明 |
|---|---|
| 引擎配置 | `SpringProcessEngineConfiguration`，共享 Spring 数据源与事务管理器 |
| 异步作业执行器 | 单机可**关闭** `asyncExecutor`，由 `xy-job` 统一驱动（避免多节点重复执行）；如开启需配 `flowable.async-executor-activate` 与锁 |
| 历史级别 | `audit`（记录全部活动）—— 监理行业需要完整留痕，这是合规要求 |
| 待办查询 | **不要实时 join `ACT_RU_TASK`**，流程事件驱动写 `biz_todo` 表，查询走自己的索引 |
| **驳回任意节点** | Flowable 原生只支持退回上一节点，需用 `ChangeActivityStateBuilder` + 自定义历史记录。**这是必做项**，中国式审批 100% 需要 |
| 会签/或签 | 多实例 + `CompletionCondition` 表达式 |
| 加签/转办 | `addCandidateUser` / `setAssignee` + 独立留痕表 |
| 超时提醒 | `xy-job` 扫描 `ACT_RU_TASK.DUE_DATE_` |
| 流程版本 | 运行中实例绑定启动时的定义版本，与表单版本化同理 |

**流程事件监听**统一用 Spring `@EventListener` 订阅 Flowable 的 `FlowableEngineEvent`，避免在业务代码里散落引擎 API 调用。

---

## 十四、统一响应、异常与错误码

```java
// xy-common
public record R<T>(int code, String message, T data) {
    public static <T> R<T> ok(T data) { return new R<>(0, "ok", data); }
    public static <T> R<T> fail(ErrorCode ec) { return new R<>(ec.getCode(), ec.getMessage(), null); }
}
```

**错误码分段**（便于定位和前端分流）：

| 区段 | 含义 | 示例 |
|---|---|---|
| 0 | 成功 | — |
| 400–499 | 通用客户端错误 | 401 未认证、403 无权限、404 不存在 |
| 1000–1999 | 认证/授权 | 1001 验证码错误、1002 账号锁定、1003 租户上下文缺失 |
| 2000–2999 | 元数据/表单 | 2001 表单不存在、2002 字段未登记、2003 校验失败 |
| 3000–3999 | 流程 | 3001 任务已被处理、3002 无审批权限 |
| 5000+ | 系统错误 | 5000 内部错误 |

**全局异常处理器**：业务异常返回业务码 + 友好提示；其余异常记完整堆栈到日志但**只返回"系统繁忙"**（不泄露堆栈，等保要求）。

**参数校验**：`@Validated` + `MethodArgumentNotValidException` 统一转成 `2003 校验失败` 并带上字段级错误列表。

---

## 十五、测试策略

低代码平台的测试难点是**组合爆炸**：不同表单配置 × 不同权限 × 不同流程状态 × 不同校验规则。

| 层次 | 做什么 | 工具 |
|---|---|---|
| **架构测试** | 依赖方向、无循环依赖 | ArchUnit（见 2.3）★ 必做 |
| **单元测试** | 校验引擎、动态查询构造器、编码规则 | JUnit 5 + AssertJ |
| **集成测试** | Service 层 + 真实数据库 | **Testcontainers**（起真 PG，不用 H2）★ 关键 |
| **安全测试** | 跨租户/跨项目越权、SQL 注入 | 参数化用例，两个账号互相访问 |
| **契约测试** | 前后端接口契约 | 与前端 `src/api/auth.ts` 的类型对齐 |
| **E2E** | 登录 → 填报 → 审批 → 打印 | Playwright（前端已就绪可接入） |

**两条硬建议**：

1. **用 Testcontainers 起真实 PostgreSQL，别用 H2**。H2 不支持 JSONB、生成列、部分索引、GIN 索引——你在测试里验证的东西和生产不是一回事，等于没测。这是低代码平台最容易自欺欺人的地方。

2. **越权测试要成为独立测试套件**。写一个基类：租户 A 的用户访问租户 B 的所有资源，断言全部 403/404。这类漏洞一旦上生产就是安全事故。

---

## 十六、建议的第一个里程碑（M1）

前端登录页已完成，后端 M1 的目标是**让它真正登录进去**。

| 序号 | 任务 | 交付 |
|---|---|---|
| 1 | `xy-parent` + 5 个模块骨架 + Flyway + Docker Compose（PG/Redis） | 能跑起来 |
| 2 | `xy-framework`：统一响应、全局异常、MyBatis-Plus 配置、Redis 封装 | 基础设施就绪 |
| 3 | `xy-system`：租户/用户/角色/项目表 + 初始化数据（含一个演示账号） | 有数据可登 |
| 4 | 图形验证码接口（Redis 存储 + GETDEL 一次性消费） | `/api/auth/captcha` 可用 |
| 5 | 登录 + JWT 签发 + `/api/auth/me` + 登出 + 黑名单 | **前端能登录进工作台** |
| 6 | 多租户拦截器 + 数据权限拦截器（含 ArchUnit 测试） | 隔离机制生效 |
| 7 | 登录限流 + 失败锁定 + 审计日志 | 达到可上线标准 |
| 8 | ArchUnit + Testcontainers 测试骨架 | 后续开发的护栏 |
| 9 | Knife4j 接口文档，前端 `VITE_USE_MOCK=false` 联调通过 | **端到端打通** |

**M1 完成标志**：前端把验证码换成服务端图片、关掉 Mock，用真实账号登录成功进入工作台。

**M1 不要做的事**：不要碰元数据引擎、不要接 Flowable、不要做文件上传。**先把骨架和护栏立起来**（模块边界 + 异常体系 + 测试骨架 + 租户隔离），后面才敢快速迭代。骨架歪了，写多快都是债。

---

## 十七、依赖清单

```xml
<!-- 核心 -->
JDK 21 (LTS)
Spring Boot 3.5.x
Spring Security 6.x
MyBatis-Plus 3.5.x          (含 mybatis-plus-spring-boot3-starter)
Flowable 7.x                (flowable-spring-boot-starter-process)
PostgreSQL 16+              (驱动 + Flyway)
Flyway 10.x
Redis 7.x + Redisson 3.x    (分布式锁、限流)
MinIO 8.x
MapStruct 1.6.x
Lombok
Hutool 5.x
Aviator 5.x                 (校验规则表达式引擎，沙箱执行)

<!-- 校验与文档 -->
spring-boot-starter-validation
Knife4j 4.x / SpringDoc OpenAPI 3

<!-- 测试 -->
JUnit 5 + AssertJ + Mockito
Testcontainers (postgresql, redis)
ArchUnit 1.x
REST Assured

<!-- 可观测 -->
Micrometer + Prometheus
Logback + logstash-logback-encoder (JSON 日志)
SkyWalking Agent
```

---

## 附：与总体方案的分工

| 内容 | 去哪看 |
|---|---|
| 为什么单体/为什么这些技术选型 | 总体方案 第 3 章 |
| JSONB 建模的三路线对比、表设计 | 总体方案 第 4 章 |
| 流程引擎的用户视角设计 | 总体方案 第 5 章 |
| 移动端离线、报表归档 | 总体方案 第 6–7 章 |
| 部署拓扑、监控、安全清单 | 总体方案 第 9 章 |
| 信创、等保、电子签名等外部约束 | 总体方案 第 10 章 |
| **模块边界、认证实现、拦截器顺序、JSONB 实操、事务、测试** | **本文** |

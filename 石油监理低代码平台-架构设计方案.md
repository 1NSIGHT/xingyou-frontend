# 石油监理行业低代码平台 — 架构设计方案

> 版本：v1.0 设计基线
> 约束条件：并发量低（同时在线 ~100），支持用户自定义设计（低代码）
> 定位：由监理公司/业主的业务人员自行设计表单、流程、报表，避免每个项目做二次开发

---

## 一、先给结论（TL;DR）

| 决策项 | 结论 | 一句话理由 |
|---|---|---|
| 架构形态 | **模块化单体（Modular Monolith）** | 100 并发是"小系统"，微服务/K8s 是纯粹的负债 |
| 后端 | **JDK 21 + Spring Boot 3.5.x + Maven 多模块** | 信创适配最成熟 + 低代码所需组件生态最全 |
| 数据库 | **PostgreSQL 16+**（信创要求下达梦8/人大金仓） | JSONB + GIN + 生成列，是低代码动态建模的最优解 |
| 低代码引擎 | **百度 amis 6.x（渲染引擎）+ amis-editor（设计器）** | JSON 驱动，天然适合表单/列表/审批密集场景 |
| 动态数据建模 | **固定物理表 + JSONB 扩展列 + 生成列索引 + 报表展平宽表** | 绝不动态 DDL，绝不用纯 EAV |
| 流程引擎 | **Flowable 7.x**（用户画简化审批流，后端编译成 BPMN） | 轻量、BPMN 标准、国产化案例多 |
| 前端 | **Vue 3 + TS + Vite + Element Plus + amis** | 招人容易，生态成熟 |
| 移动端 | **同一套 JSON Schema → H5 渲染器 + 小程序壳 + PWA 离线队列** | 现场无信号是石油监理的刚需 |
| 报表 | **积木报表 JimuReport**（免费）+ **Word 模板占位符（poi-tl）** | 中国式报表 + 固定格式报验单两条腿 |
| 部署 | **Docker Compose，2×4C8G 应用 + PG 主从 + Redis + MinIO** | 单机都够，预留容器化便于将来上信创云 |
| 起步方式 | **自研元数据引擎 + 参考 JeecgBoot/若依做底座** | 纯买商业平台会死在行业定制和数据安全上 |

**最重要的一句话**：这个项目的成败 90% 不在技术，而在**业务抽象**。你必须先有一个真正懂监理业务的业务架构师，把"旁站记录 / 见证取样 / 质量控制点 / 焊口台账 / 整改闭环"抽象成通用的"单据 + 流程 + 台账 + 报表"模型。技术选型做错了可以换，业务抽象做错了就是推倒重来。

---

## 二、行业业务分析（架构的输入）

### 2.1 石油监理的核心业务特征

石油工程监理覆盖：长输管道、油气田地面工程、炼化装置、储气库、LNG 接收站。核心业务单据包括：

| 业务域 | 典型单据 | 频次 | 特征 |
|---|---|---|---|
| 监理策划 | 监理规划、监理实施细则 | 低 | 长文档、审批流 |
| 现场记录 | **旁站监理记录**、监理日志、监理周报 | **极高** | 移动端填报、拍照、GPS、离线 |
| 质量验收 | 平行检验记录、**隐蔽工程验收**、工序质量报验（A/B/C 级控制点，停检点/必检点/巡检点） | 极高 | 强流程、强留痕、需签字盖章 |
| 见证取样 | 见证取样送检台账、委托单 | 高 | 台账 + 编号规则 |
| 管道专项 | **焊口台账**（焊口编号/焊工/日期/RT·UT 无损检测结果）、防腐补口记录 | 极高 | 每道焊口一条，一个项目几万条，需二维码追溯 |
| 问题闭环 | 监理通知单、工程暂停令/复工令、质量问题整改闭环 | 中高 | 闭环状态机、超时升级 |
| 变更与计量 | 工程变更、工程洽商、计量支付证书 | 中 | 多级审批、金额计算 |
| 报表归档 | 监理月报、竣工资料归档（GB/T 50328） | 低 | 汇总统计 + 固定格式输出 |

### 2.2 为什么必须做低代码 —— 三个"每个项目都不一样"

1. **每个业主的报验单格式不一样** → 需要**表单自定义**
2. **每个业主的审批层级和条件不一样**（有的要总监批，有的要业主项目部批，有的金额超 5 万要升级） → 需要**流程自定义**
3. **每个业主的月报/台账格式不一样** → 需要**报表自定义**

如果不做低代码，你的产品就会退化成"给每个项目做一次二次开发"的外包公司模式，永远无法产品化。这是做这件事的根本动机。

### 2.3 组织与权限模型（三层结构）

```
监理公司 (tenant)
  └── 项目部 / 监理部 (project)          ← 数据隔离与统计的主维度
        └── 标段 / 单位工程 (section)     ← 行级数据权限粒度
              └── 专业 (土建/工艺/焊接/电气仪表/无损检测)

角色：系统管理员 / 公司管理层 / 总监理工程师 / 专业监理工程师 / 监理员
      / 业主(只读+审批) / 施工方(仅提交) / 检测单位(仅回填结果)
```

关键点：**多项目并行**是常态，一个监理公司同时管几十个项目。所以 `project_id` 是一等公民，几乎每张业务表都要带，且是所有列表/报表的默认过滤维度。

---

## 三、总体架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│  客户端层                                                            │
│  PC 浏览器(设计器 + 业务端)  │  移动 H5/PWA  │  微信小程序  │  大屏    │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ HTTPS
┌──────────────────────────────▼──────────────────────────────────────┐
│  接入层   Nginx (TLS 终止 / 静态资源 / 限流 / WAF 规则)               │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│  应用层   Spring Boot 3.5 模块化单体  (2 节点，无状态，可水平扩)      │
│                                                                      │
│  【平台内核】                                                         │
│   xy-auth 认证鉴权  │ xy-system 组织/用户/角色/项目/字典/租户         │
│   xy-meta ★元数据引擎  │ xy-form ★表单数据引擎  │ xy-flow ★流程引擎   │
│   xy-report 报表与归档  │ xy-file 文件/照片/签名  │ xy-portal 渲染API │
│                                                                      │
│  【行业业务】                                                         │
│   xy-biz-supervision  焊口台账 / 质量控制点 / 见证取样 / 整改闭环     │
│                                                                      │
│  【支撑】  xy-common 基础  │  xy-job 定时任务 / ETL 展平 / 超时提醒   │
└───┬──────────────┬───────────────┬──────────────┬───────────────────┘
    │              │               │              │
┌───▼────┐  ┌──────▼────┐  ┌───────▼─────┐  ┌────▼──────────┐
│Postgres│  │  Redis 7  │  │   MinIO     │  │ SkyWalking /  │
│主 + 从 │  │缓存/会话/锁│  │照片/附件/PDF│  │Prometheus/Grafana│
└────────┘  └───────────┘  └─────────────┘  └───────────────┘
```

### 3.2 为什么是单体而不是微服务（这段很重要，别被带偏）

| 维度 | 100 并发下的现实 |
|---|---|
| 峰值 QPS | 约 50–200（100 在线用户，人均 0.5–2 req/s） |
| 单机能力 | 一台 8C16G 跑 Spring Boot + PG，轻松扛住 500+ QPS |
| 微服务的成本 | 服务拆分 + 注册中心 + 配置中心 + 网关 + 链路追踪 + 分布式事务 + 多份部署运维 |
| 微服务的收益 | 独立扩缩容、独立发布、团队自治 —— **这三个收益在 3–6 人团队、单产品线场景下全部为零** |

**结论**：模块化单体。用 Maven 多模块强制**编译期边界**（`xy-meta` 不允许直接依赖 `xy-biz-supervision` 的内部类），保留未来拆分的可能。低代码平台的元数据引擎、表单引擎、流程引擎之间是强耦合的（一次提交要同时碰三者），拆成微服务只会引入分布式事务噩梦。

**唯一建议拆分出去的**：文件服务（MinIO 本来就是独立进程）和报表 ETL 定时任务（可独立部署，避免拖累 Web 请求）。

---

## 四、核心设计：低代码引擎（方案重点）

这是整个平台的心脏，也是最容易做错的地方。

### 4.1 设计态 → 运行态全链路

```
【设计态】业务人员操作
   表单设计器 (amis-editor 嵌入)
        │  拖拽组件、配置校验、绑定字典、设置权限
        ▼
   JSON Schema  (form_definition.schema_json)
        │  保存草稿
        ▼
   点击【发布】──→ 触发【编译】
        │
        ├─ 提取字段清单 (field_key, type, required, unique, searchable)
        ├─ 提取校验规则（后端可执行的规则 DSL）
        ├─ 提取可查询字段 → 生成 PostgreSQL 生成列 + GIN/BTREE 索引
        ├─ 生成列表视图 / 详情视图 默认 schema
        ├─ 生成权限点（formKey:read / formKey:create / formKey:field:xxx:read）
        ├─ 生成编码规则（如 XY-2025-PL01-HK-000123）
        ├─ 绑定审批流定义
        └─ 生成菜单/入口
        ▼
   写入 form_definition_version（不可变快照，version+1）

【运行态】终端用户操作
   GET /api/portal/render?formKey=X&version=N
        │  ← 命中本地 Caffeine 缓存（元数据是最高频读取，必须本地缓存）
        ▼
   amis renderer 渲染 JSON → 用户填报（前端校验仅为体验）
        │
        ▼  POST /api/form/{formKey}/submit
   后端按 schema 二次校验（★ 安全底线，前端校验绝不可信）
        │
        ├─ 幂等检查（clientRequestId）
        ├─ 唯一性检查 / 跨字段校验 / 字典有效性
        ├─ 写主表 + data_json (JSONB)
        ├─ 写子表（明细行）
        ├─ 触发 Flowable 流程实例
        ├─ 写审计日志（谁、何时、改了什么、IP、设备）
        └─ 发事件（通知、Webhook、台账同步）
```

### 4.2 动态数据建模 —— 三种路线对比（最关键的技术决策）

| 路线 | 做法 | 判定 |
|---|---|---|
| A. 动态 DDL | 每建一个表单就 `CREATE TABLE` / `ALTER TABLE ADD COLUMN` | ❌ **绝对不要**。DDL 锁表、schema 爆炸（几百张表）、版本回滚灾难、备份恢复困难、无法跨环境迁移 |
| B. 纯 EAV | `entity / attribute / value` 三张表存一切 | ❌ **不要**。查询和统计极度痛苦（一个 20 字段的列表要 join 20 次），性能随数据量崩塌 |
| C. **元数据 + JSONB 扩展列（混合）** | 物理表结构固定，自定义字段进 JSONB，热字段做生成列索引，统计走展平宽表 | ✅ **推荐** |

**路线 C 的具体落地**：

```sql
-- 通用单据主表（所有低代码表单共用一张，或按业务域分几张）
CREATE TABLE biz_document (
    id                BIGSERIAL PRIMARY KEY,
    tenant_id         BIGINT       NOT NULL,       -- 租户（监理公司）
    project_id        BIGINT       NOT NULL,       -- 项目（★ 数据权限主维度）
    section_id        BIGINT,                      -- 标段/单位工程
    form_def_id       BIGINT       NOT NULL,       -- 表单定义 ID
    form_def_version  INT          NOT NULL,       -- ★ 版本快照，保证历史单据渲染不变
    biz_no            VARCHAR(64),                 -- 业务编号（编码规则生成）
    title             VARCHAR(255),
    status            VARCHAR(32)  NOT NULL,       -- DRAFT/IN_FLOW/APPROVED/REJECTED/ARCHIVED
    data_json         JSONB        NOT NULL,       -- ★ 所有自定义字段
    -- 生成列：只对"发布时声明为可查询"的字段建，PostgreSQL 自动维护。
    -- ⚠️ 生成列只接受 IMMUTABLE 表达式，选型时必须区分：
    --    ✔ text 取值 (jsonb->>)     —— immutable，可直接用
    --    ✔ 数值转换 ::numeric       —— numeric_in 是 immutable
    --    ✔ 布尔转换 ::boolean       —— boolin 是 immutable
    --    ✘ 日期转换 ::date / ::timestamp —— date_in/timestamptz_in 是 STABLE
    --      （依赖 DateStyle 会话参数），用在生成列会报
    --      "generation expression is not immutable"
    --    日期字段的解法：ISO-8601 文本直接排序（YYYY-MM-DD 字典序 == 时间序），
    --    确需 timestamp 类型时用下方 to_ts_immutable() 包装函数。
    gen_hoop_no       TEXT GENERATED ALWAYS AS (data_json->>'hoopNo') STORED,
    gen_weld_date     TEXT GENERATED ALWAYS AS (data_json->>'weldDate') STORED,
    gen_amount        NUMERIC(18,2) GENERATED ALWAYS AS ((data_json->>'amount')::numeric) STORED,
    created_by        BIGINT       NOT NULL,
    created_at        TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at        TIMESTAMPTZ,
    deleted           BOOLEAN      NOT NULL DEFAULT false,
    version           INT          NOT NULL DEFAULT 0   -- 乐观锁
);

-- 逃生舱：确需 timestamp 生成列时用这个 IMMUTABLE 包装函数。
-- 前提是数据库/角色级 DateStyle 固定为 'ISO, YMD' 且永不更改，否则索引会失真。
CREATE OR REPLACE FUNCTION to_ts_immutable(txt TEXT)
RETURNS TIMESTAMP
LANGUAGE sql IMMUTABLE PARALLEL SAFE
AS $$ SELECT txt::timestamp $$;

CREATE INDEX idx_doc_proj_status  ON biz_document (tenant_id, project_id, form_def_id, status) WHERE NOT deleted;
CREATE INDEX idx_doc_data_gin     ON biz_document USING GIN (data_json jsonb_path_ops);  -- 兜底任意字段查询
CREATE INDEX idx_doc_hoop_no      ON biz_document (gen_hoop_no) WHERE NOT deleted;      -- 热字段精确查询
CREATE INDEX idx_doc_weld_date    ON biz_document (gen_weld_date) WHERE NOT deleted;     -- ISO 文本，可直接范围扫描
```

**子表**（明细行，如报验单的检验项、焊口的检测记录）：
```sql
CREATE TABLE biz_document_item (
    id          BIGSERIAL PRIMARY KEY,
    doc_id      BIGINT NOT NULL REFERENCES biz_document(id),
    item_key    VARCHAR(64) NOT NULL,   -- 子表标识（一个表单可有多个子表）
    seq         INT NOT NULL,           -- 行序
    data_json   JSONB NOT NULL,
    ...
);
CREATE INDEX idx_item_doc ON biz_document_item (doc_id, item_key, seq);
```

**为什么这条路对**：
- 主表结构稳定 → 备份、迁移、分区、升级全部正常
- JSONB 无 schema 变更成本 → 用户加字段是纯数据操作，秒级
- 生成列 + 索引 → 90% 的高频查询走 BTREE，性能等同原生列
- GIN 索引兜底 → 支持 `data_json @> '{"x":1}'` 任意组合查询
- **报表统计走展平宽表** → 定时任务（xy-job）把 JSONB 展平成 `rpt_xxx` 宽表，积木报表直接查宽表，永不碰 JSONB 做聚合

**索引声明机制**（设计器里必须有的功能）：
在表单设计器里，字段属性多一个开关「**可作为查询条件/统计维度**」。打开后：
- 发布时自动生成 `gen_<field>` 生成列 + BTREE 索引
- 列表设计器里该字段可作为筛选条件
- 报表 ETL 会把该字段展平到宽表
- 有数量上限提示（如单表单最多 20 个索引字段），防止滥用

### 4.3 元数据表设计

```
form_definition              表单定义（逻辑实体，formKey 唯一）
  ├─ form_definition_version 表单定义版本（不可变快照）★ 核心
  ├─ form_field_index        字段索引声明（决定生成列/索引/检索能力）
  ├─ form_permission         表单级 + 字段级权限
  ├─ form_relation           表单间关联（主子表、引用、台账来源）
  ├─ code_rule               编码规则（流水号）
  └─ list_view               列表视图定义（列、筛选、排序、按钮）

flow_definition / flow_definition_version    审批流定义 + 版本
report_template                              报表模板
page_definition                              自定义页面（驾驶舱/首页）
```

**★ 版本化是不可妥协的**：单据实例存 `form_def_version`。当管理员修改表单并发布 v2 后，v1 的历史单据**仍然按 v1 的 schema 渲染和打印**。这是低代码平台最容易踩、也最致命的坑 —— 不做版本化，用户改一次表单，所有历史单据全乱。

### 4.4 动态查询的安全构造

用户在设计器里选查询字段 → 后端构造 SQL。**必须严格白名单**：

```java
// 伪代码：查询构造器
QuerySpec spec = request.getSpec();
List<Condition> safe = spec.getConditions().stream()
    // 1. 字段必须在 form_field_index 中登记且属于当前 formKey
    .filter(c -> fieldIndexService.isQueryable(formKey, c.getField()))
    // 2. 操作符必须在允许枚举内（EQ/NE/GT/LT/IN/LIKE/BETWEEN/IS_NULL）
    .filter(c -> ALLOWED_OPS.contains(c.getOp()))
    // 3. 值必须按字段类型校验并参数化绑定（绝不字符串拼接）
    .peek(c -> c.setValue(typeCoerce(fieldType(c), c.getValue())))
    .toList();
// 4. 强制注入租户 + 数据权限谓词（不可绕过）
```

**四个必须做的防护**：
1. 字段名白名单（只允许 `data_json->>'fieldKey'` 形式，fieldKey 匹配 `^[a-zA-Z][a-zA-Z0-9_]{0,63}$`）
2. 操作符枚举白名单
3. 全部走参数化绑定，禁止字符串拼接
4. 租户/项目数据权限谓词**由框架强制注入**，不依赖业务代码

### 4.5 后端校验引擎

前端 amis 的校验只为了体验，**后端必须按 schema 重新执行全量校验**：

| 校验类型 | 说明 |
|---|---|
| 类型校验 | string/int/decimal/date/datetime/bool/array/object |
| 必填校验 | 含条件必填（`visibleWhen` 表达式为真时才必填） |
| 范围/长度/正则 | min/max/pattern |
| 枚举/字典 | 值必须存在于绑定的数据字典中 |
| 跨字段 | 如"完工日期不得早于开工日期"（表达式 DSL） |
| 唯一性 | 在 `(tenant_id, project_id, field)` 范围内唯一，如焊口编号 |
| 业务规则 | 如"A 级控制点必须有旁站记录"（行业插件提供） |
| 附件/签名 | 指定字段必须已上传/已签名 |

校验规则以 **JSON DSL** 存储，后端有一个解释器执行，不用 Groovy/JS 动态脚本（安全和性能考虑）。只有极少数复杂规则才用受限的表达式引擎（如 Aviator / SpEL 沙箱）。

---

## 五、流程引擎设计

### 5.1 用户视角 vs 系统视角

用户**不画 BPMN**（太复杂，业务人员学不会）。提供"简化审批流设计器"：

```
单据提交 → [专业监理工程师审核] → [总监理工程师审批] → 结束
                    │
                    └─ 条件：金额 > 50000 → [业主项目部审批] → 总监理工程师审批
```

节点类型：
- **审批节点**：单人 / 会签（全部通过）/ 或签（一人通过）/ 依次审批
- **条件分支**：基于表单字段的表达式（金额、控制点等级、专业）
- **抄送节点**：知会不阻塞
- **自动节点**：调用服务（生成台账、发通知、调外部 API）

后端把简化模型**编译成 BPMN 2.0 XML**，部署到 Flowable。版本化同表单：流程定义有版本，运行中的实例绑定启动时的版本。

### 5.2 必须支持的"中国式流程"能力（Flowable 原生没有，要自研扩展）

| 能力 | 实现要点 |
|---|---|
| **驳回到任意节点** | Flowable 原生只支持退回到上一节点。需用 `ChangeActivityStateBuilder` + 自定义历史记录 |
| 加签 / 转办 / 委托代办 | 动态增删任务候选人（`addCandidateUser`）+ 加签任务表 |
| 撤回（发起人撤回） | 查 `HistoricActivityInstance`，判断后续节点未被办理 |
| 超时提醒 / 自动升级 | xy-job 扫描 `ACT_RU_TASK` 的 dueDate，超时发通知或转上级 |
| 自由流程（无固定路径） | 允许节点动态指定下一处理人 |
| 多实例会签的投票统计 | 自定义 `CompletionCondition` 表达式 |
| 审批意见 + 手写签名留痕 | 任务本地变量存签名图片 ID + 意见 |

### 5.3 待办性能

100 并发下待办查询不是瓶颈，但要注意：
- 待办列表不要实时查 `ACT_RU_TASK` 的复杂 join → 建**待办视图表** `biz_todo`，流程事件驱动更新（异步），查询走自己的表和索引
- Redis 缓存用户待办数量徽标（TTL 30s）

---

## 六、前端架构

### 6.1 双端结构

```
apps/web-admin       平台管理端 + 设计器（Vue3 + amis-editor）
apps/web-business    业务运行端（Vue3 + amis renderer）
apps/mobile          移动端 H5/PWA（Vue3 + 自研轻量 schema renderer）
packages/schema-core ★ 共享的 schema 类型定义 + 校验逻辑（前后端同构）
packages/industry-widgets ★ 行业自定义物料
```

### 6.2 行业自定义物料（amis 的扩展点，也是你的护城河）

amis 内置组件覆盖 80%，剩下 20% 是行业特色，必须自研物料：

| 物料 | 用途 |
|---|---|
| **焊口选择器** | 按管线号/桩号/焊口编号筛选，返回焊口台账记录 |
| **桩号/里程输入** | 管道专业专用，支持 K0+000 格式，自动计算距离 |
| **坐标拾取** | 地图上打点，自动填经纬度（GIS 集成） |
| **图纸标注** | 上传图纸后在上面画圈标注问题位置 |
| **手写签名** | Canvas 手写 → PNG + 时间戳 + 哈希 |
| **拍照/水印相机** | 调用摄像头，自动叠加时间/GPS/项目名水印 |
| **二维码扫描** | 扫码定位焊口/设备，跳转其全生命周期 |
| **检测结果录入** | RT/UT 无损检测结果专用（片子编号、评定等级、结论） |
| **字典树选择** | 单位工程/分部/分项/检验批 四级联动（GB 标准） |
| **关联单据选择** | 选一张报验单，带出其子表数据 |

### 6.3 移动端与离线（石油监理刚需）

现场现实：戈壁、山区、海上平台、长输管道沿线 —— **没有信号是常态**。

**方案：PWA + IndexedDB 离线队列**

```
填报 → 本地写 IndexedDB（立即成功，用户体验好）
     → 进入同步队列（状态：待同步）
     → Service Worker 监听 online 事件 / 定时重试
     → 批量提交到 /api/form/batch-submit
     → 服务端按 clientRequestId 幂等去重
     → 返回结果，更新本地状态（成功/冲突/失败）
```

**冲突策略（务实版）**：
- 单据级乐观锁：提交时带 `version`，服务端版本不一致则返回 409，提示用户选择"覆盖/放弃/另存"。
- 离线新建的单据用客户端生成的 UUID 作为幂等键，服务端唯一索引兜底。
- **不要做字段级 CRDT 合并**，监理场景下完全没必要，纯属过度设计。

**照片处理**：
- 客户端 canvas 压缩到 ≤ 500KB（长边 1920）
- 保留 EXIF（GPS + 拍摄时间）—— 这是**监理证据链的关键**
- 分片上传 + 断点续传（MinIO multipart）
- 上传成功后原图归档，缩略图用于列表展示

**技术选型**：优先 `uni-app (Vue3)` 一套代码出 H5 + 微信小程序；如果团队 Vue 熟，也可以 H5 用 Vue3 + 小程序用 webview 壳包 H5。**不建议原生 App**（更新成本高、业主要求装 App 阻力大）。

---

## 七、报表与归档

### 7.1 两条腿走路

| 类型 | 方案 | 说明 |
|---|---|---|
| **动态统计报表**（台账、汇总、月报数据） | **积木报表 JimuReport** | 免费、国产、在线拖拽设计、支持 PDF/Excel/Word/大屏。业务人员自助改格式 |
| | 备选：帆软 FineReport | 收费但成熟稳定，如果客户指定就用 |
| **固定格式单据**（报验单、监理通知单、旁站记录打印） | **Word 模板 + poi-tl** | 业主给的就是 Word 模板。用占位符 `{{hoopNo}}` 替换，保留公章/签名位 |
| **PDF 输出** | LibreOffice headless 转 PDF | 免费、可容器化。追求极致格式可换 Aspose（收费） |
| **竣工归档** | 按 GB/T 50328 目录规范打包 | 项目 → 单位工程 → 分部 → 案卷，生成 ZIP + 目录清单 |

### 7.2 报表数据链路

```
biz_document (JSONB)
      │  xy-job 定时 ETL（每 5 分钟增量 / 每日全量）
      ▼
rpt_document_flat  展平宽表（每个高频表单一张，列 = 索引字段）
      │
      ├──→ 积木报表查宽表（快，永不聚合 JSONB）
      ├──→ 驾驶舱 / ECharts 图表
      └──→ Excel 导出 (EasyExcel 流式，防 OOM)
```

**为什么必须展平**：对 JSONB 做 `GROUP BY` + 聚合，几十万条就会明显慢。展平成普通列 + 索引后，报表查询是毫秒级。这是路线 C 的配套措施，缺了它整个方案性能会崩。

### 7.3 打印易被低估

业主的打印格式千奇百怪（套打、多栏、横向 A3、骑缝章位置）。**这是工作量最大的模块之一**。建议：
- 项目启动后**第一件事**就是收集各业主的真实 Word/Excel 打印模板样例
- 排期里给打印至少留 3–4 周
- 不要承诺"任意格式一键生成"，承诺"常见格式快速配置 + 特殊格式定制"

---

## 八、多租户与数据权限

### 8.1 租户隔离策略

**共享库共享表 + tenant_id**（100 并发场景，共享表足够，且跨项目/跨公司统计是刚需）。

- MyBatis-Plus `TenantLineInnerInterceptor` 自动注入 `tenant_id` 条件
- 所有业务表必须有 `tenant_id`，且在**实体基类**里统一定义，避免遗漏
- 白名单机制：系统表（字典、菜单、表单定义）中，全局共享的排除租户过滤
- **数据权限谓词由框架强制注入**，业务代码无法绕过 —— 这是安全红线

### 8.2 数据权限模型

```
权限维度：
  租户级    → 只能看自己公司数据
  项目级    → 只能看自己参与的项目（user_project 关联表）
  标段级    → 只能看自己负责的标段/单位工程
  专业级    → 焊接监理工程师只能看焊接类单据
  字段级    → 敏感字段（金额、供应商报价）按角色隐藏
  操作级    → 谁能提交 / 谁能审批 / 谁能在什么状态下编辑

实现：@DataScope 注解 + MyBatis 拦截器，把权限规则编译成 SQL 谓词：
       AND project_id IN (SELECT project_id FROM sys_user_project WHERE user_id = ?)
```

### 8.3 认证

- JWT（无状态）+ Redis 存刷新令牌和黑名单
- 支持对接业主统一认证（CAS/OAuth2/OIDC/LDAP）—— **石油国企基本都有统一身份认证，这是必然需求**
- 移动端长会话（现场一天不想重新登录）+ 设备绑定
- 敏感操作二次验证（电子签名、批量删除）

---

## 九、非功能性设计

### 9.1 部署拓扑（100 并发的正确规模）

```
                    Nginx (2C4G)
                         │
        ┌────────────────┴────────────────┐
   App-1 (4C8G)                      App-2 (4C8G)
   Spring Boot                       Spring Boot
        └────────────────┬────────────────┘
                         │
   ┌──────────┬──────────┼──────────┬─────────────┐
PostgreSQL  Redis 7    MinIO     监控栈        定时任务
主(4C8G)   (2C4G)     (2C4G)   (2C4G 可共用)   (可共部署)
从(4C8G) 主从复制/哨兵  对象存储
```

- **总硬件**：约 4–5 台 4C8G 虚拟机，或云上同等规格
- **Docker Compose 编排足够**，不要上 K8s
- 但**全部打 Docker 镜像**，将来客户要上信创云/K8s 时零改造
- 单机部署版（所有组件一台 8C32G）作为小客户交付选项 —— 石油行业很多项目部要求本地化部署

### 9.2 性能目标

| 指标 | 目标值 | 保障手段 |
|---|---|---|
| 首页/列表加载 | < 1.5s | 元数据本地缓存 + 分页 + 索引 |
| 表单渲染 | < 500ms | Caffeine 缓存 schema，避免每次查库 |
| 单据提交 | < 1s | 校验 + 单表写入 + 流程异步启动 |
| 报表查询 | < 3s | 查展平宽表，不聚合 JSONB |
| 并发在线 | 100（峰值 300 短时） | 2 节点 + Redis 会话 |
| 附件上传 | 5MB < 3s | MinIO + 分片 |

**关键优化点**：
1. **元数据缓存**（Caffeine 本地 + Redis 二级 + 发布时主动失效）—— 这是最高频读取
2. **待办/已办走独立表**，不实时 join Flowable 表
3. **统计走宽表**，不聚合 JSONB
4. **附件走 MinIO**，绝不存数据库 BLOB
5. **大列表强制分页**，导出用 EasyExcel 流式写出

### 9.3 可观测与运维

| 维度 | 选型 | 说明 |
|---|---|---|
| 日志 | Logback + JSON 格式 → **Loki + Grafana** | 100 并发用 Loki 比 ELK 轻得多 |
| 指标 | Micrometer + Prometheus + Grafana | JVM、连接池、慢 SQL、接口 QPS/RT |
| 链路 | **SkyWalking**（国产、信创友好） | 或 OpenTelemetry + Jaeger |
| 慢 SQL | p6spy / PostgreSQL `pg_stat_statements` | 低代码动态 SQL 必须监控，否则一条烂查询拖垮全站 |
| 备份 | PG 每日全备 + WAL 归档；MinIO 版本化 | 恢复演练每季度一次 |
| 发布 | Gitea Actions / Jenkins → Docker 滚动更新 | 内部系统，允许 2 分钟停机窗口 |
| 审计 | 独立审计日志表（不可删改） | 谁在何时对哪张单据做了什么，含 IP/设备指纹 |

### 9.4 安全清单

- [ ] 后端 schema 二次校验（前端校验只做体验）
- [ ] 动态查询字段/操作符白名单 + 全参数化
- [ ] 数据权限谓词框架强制注入
- [ ] 附件类型白名单 + 病毒扫描（可选 ClamAV）+ 私有桶 + 签名 URL 下载
- [ ] 敏感字段加密存储（身份证、手机号）+ 日志脱敏
- [ ] 接口限流（Nginx + 应用层）+ 防重放（nonce + timestamp）
- [ ] 文件上传路径穿越防护、SSRF 防护（外部 API 调用白名单）
- [ ] 电子签名合规性确认（见 10.5）

---

## 十、需要提前确认的外部约束

### 10.1 信创要求（必须最早确认）

石油国企项目**大概率有信创要求**，这直接决定技术选型：

| 组件 | 信创替代 | 影响 |
|---|---|---|
| 数据库 | PostgreSQL → **达梦 DM8 / 人大金仓 KingbaseES** | ⚠️ 两者 JSON 支持度弱于 PG！生成列、GIN 索引可能不支持。**必须在选型阶段验证**，可能需要退化为"JSON 存 TEXT + 应用层索引表" |
| 操作系统 | CentOS → **麒麟 KylinOS / 统信 UOS** | Java 友好，一般无阻碍 |
| 中间件 | Nginx → 东方通 TongWeb | Spring Boot 打包 war 可适配 |
| CPU | x86 → **鲲鹏 / 飞腾 ARM** | 需验证 Docker 镜像有 ARM 版本、性能调优 |
| 浏览器 | Chrome → **奇安信/红莲花 等国产浏览器** | ⚠️ 可能基于老 Chromium 内核，**amis/Vue3 的兼容性必须实测** |

**行动项**：项目启动第一周就发问卷确认信创清单，然后做**技术验证 Spike**（尤其数据库 JSON 能力和浏览器内核版本）。

### 10.2 数据安全与等保

- 石油行业常要求**等保三级**
- 涉及管网坐标、产能数据 → 可能需要**涉密或内网隔离部署**
- 影响：部署架构（纯内网、无外网依赖）、日志留存 6 个月、数据加密

### 10.3 电子签名法律效力

监理签字涉及工程验收责任，法律风险高：
- 简单的 Canvas 手写签名**只有"留痕"意义，法律效力有限**
- 如业主/监管部门要求，需接入 **CA 数字证书 + 时间戳服务（TSA）**，符合《电子签名法》第十三条
- **必须提前和法务/业主确认**，这是排期和成本的大变量

### 10.4 与既有系统集成

- 业主的**工程管理系统（如中石油 PMIS、中石化 ERP）** 可能有对接要求
- 需要保留**开放 API + Webhook + 数据导入导出**能力
- 预留：单点登录对接、组织架构同步、数据推送

### 10.5 老数据迁移

- 客户手里有大量 Excel 台账（焊口台账尤其）要导入
- 需要：导入模板设计 + 字段映射配置 + 校验报告 + 幂等导入（可重复执行不产生重复数据）
- **导入工具本身要作为平台功能**，不能每个项目写一次性脚本

---

## 十一、技术选型清单（可直接复制）

### 后端
```
JDK 21 (LTS)
Spring Boot 3.5.x
Spring Security 6 + JJWT
MyBatis-Plus 3.5.x（多租户插件 + 数据权限拦截器 + 分页）
Flowable 7.x（流程引擎）
PostgreSQL 16+（主）/ 达梦 DM8 / 人大金仓
Redis 7.x（缓存、会话、分布式锁 Redisson）
MinIO（对象存储，S3 兼容）
XXL-Job 或 Spring @Scheduled（定时任务）
Knife4j / SpringDoc（API 文档）
Hutool（工具集）
EasyExcel（Excel 导入导出）
poi-tl（Word 模板渲染）
Aviator（表达式引擎，校验规则 DSL）
Caffeine（本地缓存）
MapStruct + Lombok
Logback + Logstash Encoder（JSON 日志）
Testcontainers（集成测试）
```

### 前端
```
Vue 3.4+ / TypeScript 5 / Vite 5+
Pinia / Vue Router
Element Plus（业务界面）
amis 6.x + amis-editor（★ 低代码渲染器 + 设计器）
ECharts 5（图表、驾驶舱）
Axios + 请求封装
VueUse
unocss / tailwindcss（可选）
```

### 移动端
```
uni-app (Vue3) 或 Taro
PWA + Workbox（Service Worker）
idb / Dexie.js（IndexedDB 离线队列）
Vant 4（移动 UI）
```

### 报表
```
积木报表 JimuReport（免费，首选）
帆软 FineReport（收费备选）
LibreOffice headless（Office → PDF）
```

### 运维
```
Docker + Docker Compose
Nginx
Prometheus + Grafana + Loki
SkyWalking
Gitea + Gitea Actions / Jenkins
```

---

## 十二、实施路径与团队

### 12.1 分阶段计划（约 8 个月到可用产品）

| 阶段 | 周期 | 交付物 | 里程碑验收 |
|---|---|---|---|
| **0. 技术验证 Spike** | 3 周 | 信创可行性报告、amis 嵌入 PoC、JSONB 建模 PoC、浏览器兼容性实测 | 用真实业主表单跑通"设计→发布→填报→打印"闭环 |
| **1. 平台底座** | 2 个月 | 租户/组织/权限 + 元数据引擎 + 表单引擎 + amis 集成 + 基础审批流 | 用平台配置出"旁站监理记录"并真实上线一个项目 |
| **2. 行业能力** | 2.5 个月 | 焊口台账、质量控制点、见证取样、整改闭环、移动端离线、Word 打印、行业物料 | 一个真实项目全流程无纸化运行 |
| **3. 报表与归档** | 1.5 个月 | 积木报表接入、监理月报自动汇总、驾驶舱、归档打包、导入工具 | 监理月报一键生成；老 Excel 台账可批量导入 |
| **4. 产品化与治理** | 1 个月+ | 多租户完善、开放 API、监控告警、性能调优、部署自动化、文档 | 可复制交付给第二个客户 |

### 12.2 团队配置

| 角色 | 人数 | 关键要求 |
|---|---|---|
| 架构师 / 技术负责人 | 1 | **必须能写代码**，负责元数据引擎设计 |
| **业务架构师** | 1 | ★ **最关键的人**。必须懂监理业务（有监理工程师经历最好），负责业务抽象 |
| 后端工程师 | 3 | Java + Spring Boot + MyBatis-Plus，一人专攻 Flowable |
| 前端工程师 | 2–3 | 一人专攻低代码引擎/物料开发，一人业务端，一人移动端 |
| 测试工程师 | 1–2 | 低代码平台的测试重点是**组合爆炸**（不同表单配置 × 不同权限 × 不同流程） |
| 产品/实施 | 1–2 | 与业主对接、收集真实模板、配置第一个项目 |

**人力结论**：5–7 人的核心团队，8 个月左右做出可用产品。低于 5 人会明显拉长。

### 12.3 自研 vs 开源底座 —— 务实建议

**不建议从零起步**。参考现成开源项目能省 3–4 个月：

| 选项 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| **[JeecgBoot](https://github.com/jeecgboot/JeecgBoot/releases)** | Java + Spring Boot 3、在线表单设计、Online 报表、流程、多租户俱全，国产，社区活跃 | Online 表单引擎在**复杂行业场景下会受限**（复杂校验、性能、主子表深度） | ★ **作为底座起步 + 参考其元数据设计**，但核心表单/流程引擎建议自研或基于 amis 重做 |
| **RuoYi-Vue-Plus + Flowable** | 权限/多租户/数据权限成熟清爽，二次开发自由度最高 | 低代码能力需完全自研 | ★ **如果团队技术强、要做长期产品，这个更干净** |
| **[阿里 lowcode-engine](https://developer.aliyun.com/article/1670790)** | 企业级、可出码、物料体系完整 | 学习曲线陡，物料要自己建，偏"页面搭建"而非"业务单据" | 备选。适合要做复杂自定义页面时 |
| **amis** | JSON 驱动，配置即页面，**完美匹配表单/列表/审批密集场景**，编辑器可嵌入 | 极复杂的交互/性能敏感页面需手写 | ★ **首选渲染 + 设计器方案** |
| **Odoo** | 元数据驱动、低代码、成熟度极高 | Python 技术栈，信创和团队匹配度差 | 不推荐 |
| **商业平台**（明道云/简道云/宜搭/氚云） | 立刻能用，几万块/年 | 数据在别人云上（石油行业数据安全红线）、行业物料做不了、深度集成难、长期被绑定 | **仅在"验证需求"阶段试用**，不作为产品基础 |

**推荐路线**：`RuoYi-Vue-Plus（权限/多租户底座）+ amis（低代码渲染/设计器）+ 自研元数据/表单/流程引擎 + Flowable + 自研行业物料`。既省时间，又保证核心引擎可控。

---

## 十三、十大坑（血泪清单）

1. **不要每个表单建物理表**。用 JSONB 扩展列 + 生成列索引 + 展平宽表。动态 DDL 是低代码平台的头号死法。

2. **表单定义必须版本化，单据实例绑定版本**。不做这个，用户改一次表单，历史单据全乱，且无法修复。

3. **后端必须重新校验**。前端校验只做体验。低代码平台的前端是"用户可配置的"，等于完全不可信。

4. **动态查询必须字段白名单**。否则用户在设计器里填个 `1=1); DROP` 就是 SQL 注入。

5. **不要把 K8s / 微服务当"现代化"**。100 并发下它们是纯负债。现代化体现在**元数据驱动、容器化交付、可观测性、自动化测试**，不是服务数量。

6. **移动端离线是刚需，但别过度设计**。单据级乐观锁足够，不要碰 CRDT 字段级合并。

7. **打印格式工作量极大**。第一周就去收集真实模板，排期留足 3–4 周，别承诺"任意格式一键生成"。

8. **信创要求必须第一周确认**。数据库 JSON 能力（达梦/金仓）和国产浏览器内核版本是两个真实的、可能推倒方案的风险点，必须实测。

9. **电子签名法律效力要问法务**。涉及工程验收签字，可能必须上 CA 证书。

10. **报表统计绝不能聚合 JSONB**。必须有 ETL 展平宽表这一步，否则数据量一上来整个平台变慢。

**第 11 个（最重要）**：**低代码平台的成败在业务抽象，不在技术**。花足够时间研究"监理业务的元模型是什么"——单据、字段、流程、台账、报表、权限这六个概念如何正交组合。技术选型错了能换，业务抽象错了推倒重来。

---

## 附录 A：核心 API 草图

```
# 设计态
POST   /api/design/form                    新建表单定义
PUT    /api/design/form/{id}/schema        保存草稿 schema
POST   /api/design/form/{id}/publish       发布（触发编译，生成新版本）
GET    /api/design/form/{id}/versions      版本列表
POST   /api/design/form/{id}/rollback/{ver} 回滚到某版本（新建版本，不删除历史）
POST   /api/design/form/{id}/preview       设计器实时预览

POST   /api/design/flow                    新建审批流
POST   /api/design/flow/{id}/publish       发布流程（编译 BPMN 并部署）

POST   /api/design/list-view               列表视图定义
POST   /api/design/code-rule               编码规则

# 运行态（前端渲染器调用）
GET    /api/portal/render?formKey=X&version=N      取渲染 schema（强缓存）
GET    /api/portal/dict/{dictCode}                 取字典
GET    /api/portal/permission?formKey=X            取当前用户对该表单的权限

# 业务数据
POST   /api/form/{formKey}/submit                  提交单据（幂等）
PUT    /api/form/{formKey}/{id}                    更新单据
GET    /api/form/{formKey}/page                    分页查询（动态条件）
GET    /api/form/{formKey}/{id}                    单据详情（按绑定版本渲染）
POST   /api/form/{formKey}/batch-submit            离线批量同步
DELETE /api/form/{formKey}/{id}                    软删除

# 流程
GET    /api/flow/todo/page                         我的待办
GET    /api/flow/done/page                         我的已办
POST   /api/flow/task/{taskId}/approve             审批通过（含意见）
POST   /api/flow/task/{taskId}/reject              驳回（指定目标节点）
POST   /api/flow/task/{taskId}/countersign         加签
POST   /api/flow/task/{taskId}/transfer            转办
POST   /api/flow/instance/{id}/withdraw            撤回

# 文件
POST   /api/file/upload                            普通上传
POST   /api/file/multipart/init                    分片上传初始化
POST   /api/file/multipart/complete                分片合并
GET    /api/file/{id}/url                          取签名下载 URL

# 报表
POST   /api/report/render                          渲染报表数据
POST   /api/report/export/excel                    导出 Excel
POST   /api/report/export/pdf                      导出 PDF
POST   /api/report/word                            按模板生成 Word
```

## 附录 B：核心数据表清单

```
-- 平台
sys_tenant            租户（监理公司）
sys_org               组织架构
sys_user              用户
sys_role              角色
sys_user_role         用户角色
sys_permission        权限点
sys_data_scope        数据权限规则
sys_dict / sys_dict_item  数据字典
sys_project           项目
sys_project_member    项目成员（数据权限来源）
sys_section           标段/单位工程
sys_audit_log         审计日志

-- 元数据引擎
meta_form             表单定义（逻辑实体）
meta_form_version     表单定义版本（不可变快照）★
meta_field_index      字段索引声明（决定生成列/索引）
meta_form_permission  表单/字段权限
meta_form_relation    表单关联（主子表/引用/台账来源）
meta_code_rule        编码规则
meta_list_view        列表视图定义
meta_page             自定义页面

-- 流程
flow_definition / flow_definition_version   流程定义 + 版本
biz_todo              待办视图表（流程事件驱动更新）
biz_done              已办视图表
flow_signature        签名留痕

-- 业务数据
biz_document          单据主表（JSONB）★
biz_document_item     单据子表（JSONB）
biz_document_history  单据变更历史（字段级 diff）
biz_attachment        附件关联
biz_comment           审批意见 / 评论

-- 行业
sup_weld_joint        焊口台账（管道项目核心，字段固定，可直接建物理表）★
sup_qc_point          质量控制点定义（A/B/C 级）
sup_qc_record         质量控制点检查记录
sup_sample            见证取样台账
sup_rectify           整改闭环
sup_witness           旁站记录索引（正文在 biz_document）

-- 报表
rpt_document_flat     ★ 展平宽表（ETL 生成，按高频表单分表）
report_template       报表模板
report_snapshot       报表快照（归档用）
```

**注**：焊口台账 `sup_weld_joint` 建议**建独立物理表**而非走 JSONB —— 它是管道项目的核心实体，字段相对固定、查询极其高频、需要被其他单据引用。这是"混合模式"的正确用法：**高频核心实体走物理表，长尾自定义单据走 JSONB**。

---

*本文档为设计基线，具体版本号请在项目启动时锁定为当时的最新稳定版；信创相关的技术风险（数据库 JSON 能力、浏览器内核兼容性）必须在 Spike 阶段实测验证。*
